package
{
   public class TastelessRequest extends StaleLight
   {
       
      
      public function TastelessRequest(param1:int)
      {
         super(GateStupid.waitingStupid(MarkZonked.proseMachine),GateStupid.waitingStupid(GullibleCommon.proudFour));
         bladeToe.writeByte(param1);
      }
   }
}
