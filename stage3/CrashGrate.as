package
{
   public class CrashGrate extends StaleLight
   {
       
      
      public function CrashGrate(param1:Number)
      {
         super(GateStupid.waitingStupid(InviteReligion.grateCompany),GateStupid.waitingStupid(CuteConfused.unitChickens));
         bladeToe.writeByte(int(CuteConfused.crackerAnus * param1));
      }
   }
}
