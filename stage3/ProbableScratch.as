package
{
   import lapitchnet.reseau.interfaces._IPaquetSortant;
   
   public class ProbableScratch extends StorePaint
   {
       
      
      public function ProbableScratch(param1:int, param2:_IPaquetSortant)
      {
         super(KneelDaily.agreeClammy,NervousOnerous.skinEntertaining(MarkParty.wantSofa));
         grotesqueImportant.writeShort(param1);
         param2.ecriture(grotesqueImportant);
         dressNut = NarrowPlants.chickenCurved;
      }
   }
}
