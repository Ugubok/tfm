package
{
   public class TrailTax extends InexpensiveCrib
   {
       
      
      public var whisperRequest:String;
      
      public var lightNotebook:String;
      
      public function TrailTax(param1:int, param2:String, param3:Number, param4:String, param5:int, param6:String)
      {
         super();
         instructSign = param1;
         this.whisperRequest = param2;
         if(this.whisperRequest.toLowerCase() == ReligionPear.fragileKnife || this.whisperRequest.toLowerCase() == LaborerChop.stickScratch(TrailBerry.halfNotebook))
         {
            this.whisperRequest = LaborerChop.stickScratch(CardKuruma.alluringScintillating);
         }
         airDistro = param3;
         patChivalrous = param4;
         scaleAmuse = param5;
         violetScintillating = scaleAmuse;
         this.lightNotebook = param6;
      }
      
      public function toString() : String
      {
         return FlowerAnus.harmonyGaping + this.whisperRequest + CuteNotebook.trailTrail + airDistro + LaborerChop.stickScratch(CuteNotebook.apatheticWatery) + patChivalrous + CardKuruma.proudTouch + scaleAmuse + ScaleIcy.noxiousLeg + this.lightNotebook + LaborerChop.stickScratch(SuzukaScintillating.religionBathe);
      }
   }
}
