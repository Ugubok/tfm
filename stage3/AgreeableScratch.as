package
{
   public class AgreeableScratch extends StaleLight
   {
       
      
      public function AgreeableScratch(param1:int)
      {
         super(PatWhistle.whisperTrail,GateStupid.waitingStupid(PatWhistle.whisperTrail));
         bladeToe.writeByte(param1);
      }
   }
}
