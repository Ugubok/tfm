package
{
   import flash.utils.ByteArray;
   
   public class SistersPail extends PlanKotsky
   {
       
      
      public var yellStatement:Array;
      
      public function SistersPail(param1:ByteArray)
      {
         this.yellStatement = new Array();
         super(param1);
         var _loc2_:int = GateStupid.waitingStupid(VioletPrepare.obeisantCrib);
         while(_loc2_ < CompetitionSqueamish.harmonyNotebook)
         {
            this.yellStatement.push(RobinStupid.sistersNoiseless(param1.readUTF(),false));
            _loc2_++;
         }
      }
   }
}
