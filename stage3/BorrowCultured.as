package
{
   import flash.utils.ByteArray;
   
   public class BorrowCultured extends KotskyCheck
   {
       
      
      public var learnedCold:Boolean;
      
      public function BorrowCultured(param1:ByteArray)
      {
         super(param1);
         this.learnedCold = param1.readByte() == NervousOnerous.expertRiver(MarkParty.batheDouble);
      }
   }
}
