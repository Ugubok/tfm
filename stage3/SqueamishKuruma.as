package
{
   public class SqueamishKuruma extends StaleLight
   {
       
      
      public function SqueamishKuruma(param1:int = 0)
      {
         super(GateStupid.waitingStupid(CuteConfused.trembleChivalrous),GateStupid.waitingStupid(GullibleCommon.partyEntertaining));
         bladeToe.writeInt(SqueamishStatement.lookBorrow.scintillatingOrder);
         bladeToe.writeByte(param1);
      }
   }
}
