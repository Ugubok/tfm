package
{
   public class SpoonBalance extends StorePaint
   {
       
      
      public function SpoonBalance(param1:int)
      {
         super(BalanceSecret.nearWealthy,NervousOnerous.unwrittenProgram(MarkParty.cyclePunch));
         roomBag.writeInt(param1);
      }
   }
}
