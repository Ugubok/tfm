package
{
   public class WhistleDeadpan extends StaleLight
   {
       
      
      public function WhistleDeadpan(param1:int)
      {
         super(GateStupid.waitingStupid(EliteProse.deliverInexpensive),GateStupid.waitingStupid(EliteProse.zonkedOrange));
         bladeToe.writeShort(param1);
      }
   }
}
