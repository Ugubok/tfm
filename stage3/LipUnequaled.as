package
{
   public class LipUnequaled extends StaleLight
   {
       
      
      public function LipUnequaled(param1:int)
      {
         super(GateStupid.waitingStupid(GullibleCommon.partyEntertaining),GateStupid.waitingStupid(PatWhistle.cribNotebook));
         bladeToe.writeInt(param1);
      }
   }
}
