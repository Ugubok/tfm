package
{
   public class RealKey extends StorePaint
   {
       
      
      public function RealKey(param1:String, param2:Boolean)
      {
         super(BalanceSecret.firstParty,RepulsiveDear.lateSnotty);
         wrathfulElite.writeUTF(param1);
         wrathfulElite.writeByte(!!param2?int(MarkParty.hugeStomach):int(FaithfulBaseball.beadMighty));
      }
   }
}
