package
{
   public class HatefulWarlike extends StaleLight
   {
       
      
      public function HatefulWarlike(param1:int)
      {
         super(GateStupid.waitingStupid(PlanAgreeable.flowerKotsky),GateStupid.waitingStupid(VioletPrepare.whisperStatement));
         bladeToe.writeByte(param1);
      }
   }
}
