package
{
   public class SeedLabel extends StaleLight
   {
       
      
      public function SeedLabel(param1:int)
      {
         super(GullibleCommon.partyEntertaining,GateStupid.waitingStupid(BuryClub.competitionCrown));
         bladeToe.writeInt(param1);
      }
   }
}
